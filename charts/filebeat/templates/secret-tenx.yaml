{{- if and .Values.tenx.enabled .Values.tenx.apiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "filebeat.fullname" . }}-tenx-api-key
  labels:
    app: {{ template "filebeat.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    heritage: {{ .Release.Service | quote }}
    release: {{ .Release.Name | quote }}
type: Opaque
data:
  api-key: {{ .Values.tenx.apiKey | b64enc | quote }}
{{- end }}
{{- if and .Values.tenx.enabled .Values.tenx.gitToken }}
{{- if .Values.tenx.apiKey }}
---
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "filebeat.fullname" . }}-tenx-git-token
  labels:
    app: {{ template "filebeat.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    heritage: {{ .Release.Service | quote }}
    release: {{ .Release.Name | quote }}
type: Opaque
data:
  token: {{ .Values.tenx.gitToken | b64enc | quote }}
{{- end }}
